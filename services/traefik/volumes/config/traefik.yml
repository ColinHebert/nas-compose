http:
  routers:
    traefik:
      rule: Host(`traefik.dev.dedicated.contact`)
      service: api@internal
      middlewares:
        - traefik-auth
  middlewares:
    traefik-auth:
      basicAuth:
        usersFile: /run/secrets/traefik_password
