http:
  routers:
    traefik:
      rule: Host(`traefik.{{ env `NASCOMPOSE_DOMAIN` }}`)
      service: api@internal
      middlewares:
        - traefik-auth
  middlewares:
    traefik-auth:
      basicAuth:
        usersFile: /run/secrets/traefik_password
